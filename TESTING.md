# 🧪 Инструкция по тестированию AI Voice Hostess Bot

## Предварительные требования

1. **Docker & Docker Compose** установлены
   - Windows: [Docker Desktop](https://www.docker.com/products/docker-desktop/)
   - Linux: `sudo apt install docker.io docker-compose`
   - macOS: [Docker Desktop](https://www.docker.com/products/docker-desktop/)

2. **API ключи** (хотя бы один провайдер):
   - Claude: https://console.anthropic.com/
   - OpenAI: https://platform.openai.com/api-keys
   - Yandex: https://console.cloud.yandex.ru/

## 🚀 Быстрый старт

### 1. Настройка окружения

```bash
# Скопировать пример конфигурации
cp .env.example .env

# Отредактировать .env файл
# ВАЖНО: Добавить реальный API ключ для LLM провайдера!
nano .env  # или любой текстовый редактор
```

**Минимальная конфигурация** в `.env`:
```env
LLM_PROVIDER=claude
ANTHROPIC_API_KEY=sk-ant-api03-ваш-реальный-ключ

RESTAURANT_NAME=Гастрономия
RESTAURANT_PHONE=+7-495-123-45-67
RESTAURANT_ADDRESS=Москва, ул. Примерная, д. 1
```

### 2. Запуск системы (Development режим)

```bash
# Запустить все сервисы
docker-compose -f docker-compose.dev.yml up --build

# Или в фоновом режиме
docker-compose -f docker-compose.dev.yml up -d --build
```

### 3. Проверка запуска

После запуска проверьте, что все контейнеры работают:

```bash
docker ps
```

Должны быть запущены:
- `hostess-postgres-dev` (порт 5432)
- `hostess-redis-dev` (порт 6379)
- `hostess-backend-dev` (порт 8000)
- `hostess-frontend-dev` (порт 3000)

### 4. Проверка логов

```bash
# Все логи
docker-compose -f docker-compose.dev.yml logs -f

# Только backend
docker-compose -f docker-compose.dev.yml logs -f backend

# Только frontend
docker-compose -f docker-compose.dev.yml logs -f frontend
```

## 🧪 Тестирование

### Шаг 1: Health Check API

```bash
# Проверка базового health check
curl http://localhost:8000/api/health

# Ожидаемый ответ:
# {
#   "status": "healthy",
#   "service": "AI Voice Hostess Bot API",
#   "version": "1.0.0"
# }

# Проверка подключения к базе данных
curl http://localhost:8000/api/health/db

# Ожидаемый ответ:
# {
#   "status": "healthy",
#   "database": "connected"
# }
```

### Шаг 2: API документация

Откройте в браузере:
- **Swagger UI**: http://localhost:8000/docs
- **ReDoc**: http://localhost:8000/redoc

### Шаг 3: Prompts API

```bash
# 1. Получить все промпты
curl http://localhost:8000/api/prompts/

# 2. Получить активный промпт
curl http://localhost:8000/api/prompts/active

# 3. Получить доступные переменные
curl http://localhost:8000/api/prompts/variables/available

# 4. Hot reload промптов
curl -X POST http://localhost:8000/api/prompts/reload
```

### Шаг 4: Chat API

```bash
# Отправить сообщение боту
curl -X POST http://localhost:8000/api/chat/message \
  -H "Content-Type: application/json" \
  -d '{
    "message": "Здравствуйте, хочу забронировать столик",
    "conversation_id": null,
    "generate_audio": false
  }'

# Ожидаемый ответ:
# {
#   "response": "Здравствуйте! Конечно, я помогу вам забронировать столик...",
#   "conversation_id": "uuid-here",
#   "message_id": "uuid-here",
#   "latency_ms": 1234
# }

# Получить историю разговоров
curl http://localhost:8000/api/chat/history?limit=10&offset=0

# Получить конкретный разговор
curl http://localhost:8000/api/chat/conversation/{conversation_id}
```

### Шаг 5: Frontend UI

1. **Откройте Frontend**: http://localhost:3000

2. **Проверьте все страницы**:
   - 💬 **Chat** (http://localhost:3000/)
     - Отправка сообщений
     - Получение ответов от бота
     - Отображение истории в текущей сессии
     - Кнопка "Новый разговор"

   - 📝 **Prompts** (http://localhost:3000/prompts)
     - Список промптов
     - Monaco Editor
     - Редактирование и сохранение
     - Preview с переменными
     - Hot Reload кнопка

   - 📜 **History** (http://localhost:3000/history)
     - Список разговоров
     - Просмотр деталей
     - Отображение сообщений

   - 📊 **Dashboard** (http://localhost:3000/dashboard)
     - Метрики (разговоры, сообщения)
     - Среднее время ответа
     - Последние разговоры

## 🐛 Проверка типичных проблем

### Проблема 1: Backend не стартует

```bash
# Проверить логи
docker-compose -f docker-compose.dev.yml logs backend

# Частые причины:
# - Неверный API ключ LLM провайдера
# - База данных не готова (подождите 10-20 сек)
# - Порт 8000 уже занят
```

**Решение**:
- Проверьте `.env` файл - API ключ должен быть реальным
- Подождите, пока PostgreSQL полностью запустится
- Освободите порт 8000 или измените его в docker-compose.dev.yml

### Проблема 2: Frontend не подключается к Backend

```bash
# Проверить переменные окружения frontend
docker-compose -f docker-compose.dev.yml logs frontend
```

**Решение**:
- В браузере откройте DevTools (F12) → Console
- Проверьте, что API запросы идут на http://localhost:8000
- Убедитесь, что CORS настроен правильно в backend

### Проблема 3: LLM не отвечает

```bash
# Проверить логи backend
docker-compose -f docker-compose.dev.yml logs backend | grep -i error
```

**Решение**:
- Проверьте API ключ в `.env`
- Убедитесь, что выбран правильный `LLM_PROVIDER` (claude/openai/yandex)
- Проверьте баланс API ключа у провайдера

### Проблема 4: Database connection failed

```bash
# Проверить статус PostgreSQL
docker-compose -f docker-compose.dev.yml ps postgres

# Проверить логи
docker-compose -f docker-compose.dev.yml logs postgres
```

**Решение**:
- Подождите 30 секунд - база данных может инициализироваться
- Перезапустите контейнеры: `docker-compose -f docker-compose.dev.yml restart`

## ✅ Чек-лист тестирования

### Backend API
- [ ] Health check работает (`/api/health`)
- [ ] Database health check работает (`/api/health/db`)
- [ ] Swagger UI доступен (`/docs`)
- [ ] Можно получить список промптов (`GET /api/prompts/`)
- [ ] Можно отправить сообщение боту (`POST /api/chat/message`)
- [ ] Получен ответ от LLM (проверить latency_ms в ответе)
- [ ] Можно получить историю (`GET /api/chat/history`)
- [ ] Hot reload промптов работает (`POST /api/prompts/reload`)

### Frontend UI
- [ ] Frontend загружается на http://localhost:3000
- [ ] Chat страница отображается корректно
- [ ] Можно отправить сообщение в чате
- [ ] Получен ответ от бота в UI
- [ ] Отображается время и latency
- [ ] Prompts страница загружается
- [ ] Monaco Editor работает
- [ ] Можно редактировать промпт
- [ ] Preview показывает подстановку переменных
- [ ] History страница показывает разговоры
- [ ] Dashboard показывает метрики
- [ ] Sidebar навигация работает

### Integration
- [ ] Chat → отправка создаёт запись в History
- [ ] Dashboard показывает реальные цифры после чата
- [ ] Prompts → изменения влияют на ответы бота
- [ ] Hot reload обновляет промпты без перезапуска

## 🛑 Остановка системы

```bash
# Остановить все контейнеры
docker-compose -f docker-compose.dev.yml down

# Остановить и удалить volumes (БД будет очищена)
docker-compose -f docker-compose.dev.yml down -v
```

## 📝 Известные ограничения MVP

1. **Нет аутентификации** - любой может использовать API
2. **Нет rate limiting** - можно отправлять неограниченное количество запросов
3. **Нет RAG** - бот не использует базу знаний (будет в Фазе 4)
4. **Нет голоса** - только текстовый чат (Vocode в Фазе 5)
5. **Нет SIP** - нельзя позвонить боту по телефону (Фаза 5)

## 🎯 Следующие шаги

После успешного тестирования MVP:

1. **Фаза 4**: Добавить RAG систему (загрузка документов, векторный поиск)
2. **Фаза 5**: Интеграция Vocode + SIP телефония (опционально)
3. **Production**: Деплой на сервер с SSL, мониторингом, бэкапами

## 📞 Поддержка

При возникновении проблем:
1. Проверьте логи: `docker-compose -f docker-compose.dev.yml logs`
2. Проверьте `.env` файл
3. Убедитесь, что API ключи валидные
4. Создайте issue в репозитории с логами и описанием проблемы
