# System Prompts

Эта директория содержит системные промпты для AI бота-хостес.

## Структура

- `system_prompt_v1.txt` - Дефолтный промпт (версия 1)
- Вы можете добавить больше промптов для тестирования

## Переменные

Система автоматически подставляет следующие переменные:

- `{restaurant_name}` - Название ресторана (из .env)
- `{restaurant_phone}` - Телефон ресторана (из .env)
- `{restaurant_address}` - Адрес ресторана (из .env)
- `{date}` - Текущая дата (дд.мм.гггг)
- `{time}` - Текущее время (чч:мм)
- `{day_of_week}` - День недели (например, "Понедельник")

## Как использовать

1. **Редактировать через UI**:
   - Откройте http://localhost:3000/prompts
   - Выберите промпт
   - Редактируйте в Monaco Editor
   - Сохраните изменения

2. **Редактировать файл напрямую**:
   - Измените файл `system_prompt_v1.txt`
   - Нажмите кнопку "🔥 Hot Reload" в UI
   - Или вызовите API: `POST /api/prompts/reload`

3. **Создать новый промпт**:
   - Скопируйте `system_prompt_v1.txt` → `system_prompt_v2.txt`
   - Внесите изменения
   - Система автоматически обнаружит новый промпт

## Советы по написанию промптов

1. **Будьте конкретны**: Чётко опишите роль и задачи бота
2. **Используйте примеры**: Покажите, как отвечать в разных ситуациях
3. **Устанавливайте ограничения**: Длина ответа, стиль общения
4. **Тестируйте**: Используйте Preview для проверки подстановки переменных

## Hot Reload

Промпты загружаются при старте и могут быть перезагружены на лету:
- Через UI: кнопка "🔥 Reload" на странице Prompts
- Через API: `POST /api/prompts/reload`
- Без перезапуска backend сервиса!
