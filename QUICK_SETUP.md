# 🚀 Быстрая установка за 2 команды

## Шаг 1: Подключитесь к серверу

```bash
ssh root@217.26.27.210
```
*Введите ваш пароль*

## Шаг 2: Запустите установку

Скопируйте и вставьте эту команду:

```bash
curl -sSL https://raw.githubusercontent.com/Vovanwotkd/ai-voice/main/setup-server.sh | bash
```

Скрипт автоматически:
- ✅ Установит Docker и Docker Compose
- ✅ Установит Nginx
- ✅ Настроит SSL сертификат для voice.matrixcontrol.ru
- ✅ Настроит firewall
- ✅ Склонирует проект
- ✅ Создаст .env файл

**Во время установки:**
- Когда попросит API ключ - вставьте ваш Anthropic ключ
- Когда попросит подтвердить SSL - нажмите Enter

## Шаг 3: Запустите приложение

```bash
cd /root/ai-voice
docker-compose up -d --build
```

## Готово! 🎉

Откройте в браузере:
- **https://voice.matrixcontrol.ru**

---

## Если нужно изменить настройки

```bash
nano /root/ai-voice/.env
```

Сохранить: `Ctrl+X`, затем `Y`, затем `Enter`

Перезапустить:
```bash
cd /root/ai-voice
docker-compose restart
```

---

## Проверка статуса

```bash
# Статус контейнеров
docker-compose ps

# Логи
docker-compose logs -f

# Здоровье API
curl http://localhost:8000/api/health
```
