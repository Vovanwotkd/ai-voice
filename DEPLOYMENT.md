# 🚀 Production Deployment Guide

Полная инструкция по деплою AI Voice Hostess Bot на ваш сервер через GitHub Actions.

**Ваш сервер**: `root@217.26.27.210`

---

## 📋 Что нужно сделать

1. Подготовить сервер (установить Docker)
2. Настроить SSH ключ для доступа
3. Настроить GitHub Secrets
4. Создать .env файл на сервере
5. Запушить код - автоматический деплой!

---

## Шаг 1: Подготовка сервера

### 1.1 Подключитесь к серверу

```bash
ssh root@217.26.27.210
```

### 1.2 Установите Docker и Docker Compose

```bash
# Обновить пакеты
apt update && apt upgrade -y

# Установить зависимости
apt install -y apt-transport-https ca-certificates curl software-properties-common

# Добавить Docker GPG ключ
curl -fsSL https://download.docker.com/linux/ubuntu/gpg | apt-key add -

# Добавить Docker репозиторий
add-apt-repository "deb [arch=amd64] https://download.docker.com/linux/ubuntu $(lsb_release -cs) stable"

# Установить Docker
apt update
apt install -y docker-ce docker-ce-cli containerd.io

# Установить Docker Compose
curl -L "https://github.com/docker/compose/releases/latest/download/docker-compose-$(uname -s)-$(uname -m)" -o /usr/local/bin/docker-compose
chmod +x /usr/local/bin/docker-compose

# Проверить установку
docker --version
docker-compose --version
```

### 1.3 Установите Git

```bash
apt install -y git
git --version
```

---

## Шаг 2: Настройка SSH ключа

### 2.1 Сгенерируйте SSH ключ (на вашем локальном компьютере)

```bash
# Windows (PowerShell) или Linux/Mac (Terminal)
ssh-keygen -t ed25519 -C "github-actions-deploy" -f ~/.ssh/ai-voice-deploy

# Это создаст два файла:
# - ~/.ssh/ai-voice-deploy (приватный ключ)
# - ~/.ssh/ai-voice-deploy.pub (публичный ключ)
```

### 2.2 Добавьте публичный ключ на сервер

```bash
# Скопируйте публичный ключ
cat ~/.ssh/ai-voice-deploy.pub

# Подключитесь к серверу
ssh root@217.26.27.210

# Добавьте ключ в authorized_keys
mkdir -p ~/.ssh
echo "ВСТАВЬТЕ_ПУБЛИЧНЫЙ_КЛЮЧ_СЮДА" >> ~/.ssh/authorized_keys
chmod 700 ~/.ssh
chmod 600 ~/.ssh/authorized_keys

# Выйдите с сервера
exit
```

### 2.3 Проверьте SSH подключение

```bash
ssh -i ~/.ssh/ai-voice-deploy root@217.26.27.210

# Если всё работает - отлично!
exit
```

---

## Шаг 3: Настройка GitHub Secrets

### 3.1 Откройте настройки репозитория

1. Перейдите: https://github.com/Vovanwotkd/ai-voice/settings/secrets/actions
2. Нажмите **"New repository secret"**

### 3.2 Добавьте следующие секреты

#### `SERVER_HOST`
```
217.26.27.210
```

#### `SERVER_USER`
```
root
```

#### `SSH_PRIVATE_KEY`
```bash
# Скопируйте содержимое приватного ключа:
cat ~/.ssh/ai-voice-deploy

# Вставьте ВСЁ содержимое файла, включая:
# -----BEGIN OPENSSH PRIVATE KEY-----
# ...содержимое ключа...
# -----END OPENSSH PRIVATE KEY-----
```

#### `ANTHROPIC_API_KEY` (обязательно!)
```
sk-ant-api03-ваш-реальный-API-ключ
```
Получить: https://console.anthropic.com/

#### `OPENAI_API_KEY` (опционально)
```
sk-proj-ваш-OpenAI-ключ
```
Получить: https://platform.openai.com/api-keys

#### `YANDEX_API_KEY` (опционально)
```
ваш-Yandex-API-ключ
```
Получить: https://console.cloud.yandex.ru/

#### `YANDEX_FOLDER_ID` (опционально)
```
ваш-folder-id
```

---

## Шаг 4: Создание .env файла на сервере

### 4.1 Подключитесь к серверу

```bash
ssh root@217.26.27.210
```

### 4.2 Создайте директорию и .env файл

```bash
# Создать директорию проекта
mkdir -p /root/ai-voice
cd /root/ai-voice

# Создать .env файл
nano .env
```

### 4.3 Вставьте конфигурацию

```env
# ==========================================
# Database Configuration
# ==========================================
DB_USER=postgres
DB_PASSWORD=ВАШ_БЕЗОПАСНЫЙ_ПАРОЛЬ_ДЛЯ_БД
DATABASE_URL=postgresql://postgres:ВАШ_БЕЗОПАСНЫЙ_ПАРОЛЬ_ДЛЯ_БД@postgres:5432/hostess_db

# ==========================================
# Redis Configuration
# ==========================================
REDIS_URL=redis://redis:6379/0

# ==========================================
# LLM Provider Configuration
# ==========================================
LLM_PROVIDER=claude

# ВАЖНО: Замените на ваш реальный API ключ!
ANTHROPIC_API_KEY=sk-ant-api03-ВАШ-РЕАЛЬНЫЙ-КЛЮЧ

# Опционально (если используете)
OPENAI_API_KEY=
YANDEX_API_KEY=
YANDEX_FOLDER_ID=

# ==========================================
# Restaurant Information
# ==========================================
RESTAURANT_NAME=Гастрономия
RESTAURANT_PHONE=+7-495-123-45-67
RESTAURANT_ADDRESS=Москва, ул. Примерная, д. 1

# ==========================================
# Application Configuration
# ==========================================
# Сгенерируйте: openssl rand -hex 32
SECRET_KEY=ЗАМЕНИТЕ_НА_СЛУЧАЙНУЮ_СТРОКУ_32_СИМВОЛА

DEBUG=false

# ВАЖНО: Замените на ваш домен!
CORS_ORIGINS=http://217.26.27.210:3000,http://localhost:3000
```

**Сохраните файл**: `Ctrl+X`, затем `Y`, затем `Enter`

### 4.4 Защитите .env файл

```bash
chmod 600 .env
```

---

## Шаг 5: Запуск автодеплоя

Теперь всё готово! При каждом пуше в `main` ветку GitHub Actions автоматически задеплоит приложение.

### 5.1 Запушьте код

```bash
# На вашем локальном компьютере
git add .
git commit -m "Setup production deployment"
git push origin main
```

### 5.2 Следите за процессом деплоя

1. Откройте: https://github.com/Vovanwotkd/ai-voice/actions
2. Найдите запущенный workflow "Deploy to Production"
3. Кликните на него, чтобы видеть логи в реальном времени

### 5.3 Проверьте результат

После успешного деплоя откройте в браузере:

- **Frontend**: http://217.26.27.210:3000
- **Backend API**: http://217.26.27.210:8000
- **API Docs**: http://217.26.27.210:8000/docs

---

## 🔧 Полезные команды

### На сервере

```bash
# Подключиться к серверу
ssh root@217.26.27.210

# Перейти в проект
cd /root/ai-voice

# Посмотреть логи
docker-compose logs -f

# Посмотреть статус
docker-compose ps

# Перезапустить сервисы
docker-compose restart

# Остановить всё
docker-compose down

# Запустить заново
docker-compose up -d

# Зайти в контейнер backend
docker-compose exec backend bash

# Применить миграции вручную
docker-compose exec backend alembic upgrade head
```

---

## 🐛 Troubleshooting

### Проблема: GitHub Actions не может подключиться к серверу

**Решение:**
1. Проверьте SSH_PRIVATE_KEY в GitHub Secrets
2. Убедитесь, что публичный ключ добавлен в `~/.ssh/authorized_keys` на сервере
3. Проверьте, что порт 22 открыт на сервере

### Проблема: Backend не стартует

**Решение:**
```bash
# Посмотрите логи
ssh root@217.26.27.210
cd /root/ai-voice
docker-compose logs backend

# Частые причины:
# 1. Неверный API ключ в .env
# 2. База данных не готова (подождите 30 сек)
# 3. Порты заняты
```

### Проблема: Frontend показывает ошибку подключения

**Решение:**
1. Проверьте, что backend запущен: `curl http://localhost:8000/api/health`
2. Проверьте CORS_ORIGINS в .env файле
3. Откройте браузерную консоль (F12) и посмотрите ошибки

### Проблема: LLM не отвечает

**Решение:**
1. Проверьте API ключ в .env: `cat /root/ai-voice/.env | grep ANTHROPIC`
2. Проверьте баланс на https://console.anthropic.com/
3. Посмотрите логи: `docker-compose logs backend | grep -i error`

---

## 🔒 Безопасность (TODO для продакшена)

После базового деплоя **обязательно** настройте:

1. **Nginx reverse proxy** (порты 80/443 вместо 3000/8000)
2. **SSL сертификат** (Let's Encrypt)
3. **Firewall** (ufw или iptables)
4. **Автоматические бэкапы** БД
5. **Мониторинг** (Prometheus + Grafana)

Инструкции будут добавлены позже.

---

## 🎉 Готово!

Теперь ваш AI Voice Hostess Bot работает на production сервере!

Каждый раз при `git push origin main` приложение будет автоматически обновляться.

**Следующие шаги:**
- Протестируйте все функции
- Настройте домен и SSL
- Добавьте мониторинг
- Можно переходить к Фазе 4 (RAG) или Фазе 5 (Vocode)
