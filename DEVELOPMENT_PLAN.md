# AI Voice Hostess Bot - План разработки

## Статус проекта

### ✅ Завершено

#### Фаза 1-2: Базовый чат с YandexGPT
- [x] Backend API (FastAPI + PostgreSQL + Redis)
- [x] YandexGPT интеграция
- [x] Система промптов (редактируемые в админке)
- [x] История разговоров
- [x] Frontend React UI
- [x] Docker Compose инфраструктура
- [x] Nginx reverse proxy

#### Фаза 3: Voice Chat (Push-to-Talk)
- [x] Yandex SpeechKit STT (Speech-to-Text)
- [x] Yandex SpeechKit TTS (Text-to-Speech)
- [x] WebSocket endpoint для голосовой связи
- [x] VoiceRecorder компонент (браузер)
- [x] Конвертация аудио форматов (WebM → WAV для STT)
- [x] Воспроизведение голоса (OGG Opus)

---

## 🚀 В процессе

### Фаза 4: RAG (База знаний)

**Цель:** Добавить контекстные знания из документов ресторана (меню, правила, FAQ)

#### Архитектура RAG
```
Вопрос пользователя
    ↓
Векторизация (embeddings)
    ↓
Поиск в ChromaDB (top-k релевантных документов)
    ↓
Контекст + История → YandexGPT → Ответ
```

#### Задачи

**Backend:**
1. **Модели данных** (БД)
   - [x] Document model (название, тип, путь, метаданные)
   - [x] DocumentChunk model (части документа с embeddings)

2. **Сервисы**
   - [ ] `document_processor.py` - загрузка и парсинг документов
     - PDF через PyPDF2
     - DOCX через python-docx
     - TXT напрямую
   - [ ] `embeddings_service.py` - генерация векторных представлений
     - Используем OpenAI embeddings (text-embedding-3-small) или YandexGPT embeddings
   - [ ] `vector_store_service.py` - ChromaDB интеграция
     - Индексация документов
     - Семантический поиск
   - [ ] `rag_service.py` - основной RAG пайплайн
     - Поиск релевантных чанков
     - Формирование контекста для LLM

3. **API endpoints**
   - [ ] `POST /api/documents/upload` - загрузка документа
   - [ ] `GET /api/documents` - список документов
   - [ ] `DELETE /api/documents/{id}` - удаление документа
   - [ ] `POST /api/documents/{id}/reindex` - переиндексация

**Frontend:**
1. **Страница управления документами**
   - [ ] Загрузка файлов (drag & drop)
   - [ ] Список загруженных документов
   - [ ] Удаление документов
   - [ ] Статус индексации

**Инфраструктура:**
- [ ] ChromaDB контейнер в docker-compose
- [ ] Volume для хранения векторной БД
- [ ] Миграции для новых таблиц

**Зависимости:**
```python
chromadb==0.4.22
PyPDF2==3.0.1
python-docx==1.1.0
openai==1.7.2  # для embeddings (уже есть)
```

**Срок:** 1-2 дня

---

### Фаза 5: Vocode (Браузер + Телефония)

**Цель:** Универсальное решение для голосовых диалогов через браузер (отладка) и телефон (продакшн)

**Преимущества Vocode:**
- ✅ **WebRTC** для браузера - отладка в админке
- ✅ **SIP** для Beeline - реальные звонки
- ✅ **Streaming STT/TTS** (Yandex v3) - латентность ~300ms
- ✅ **Voice Activity Detection** - автоопределение пауз
- ✅ **Прерывания** - можно перебить бота
- ✅ Одна кодовая база для обоих каналов

#### Архитектура
```
┌──────────────┐         ┌──────────────┐
│   Браузер    │         │   Телефон    │
│   (WebRTC)   │         │ +7-XXX-XX-XX │
└──────┬───────┘         └──────┬───────┘
       │                        │
       │                   Beeline SIP
       │                        │
       └────────┬───────────────┘
                │
         ┌──────▼───────┐
         │ Vocode Agent │
         └──────┬───────┘
                │
    ┌───────────┼───────────┐
    │           │           │
┌───▼────┐ ┌───▼───┐ ┌────▼────┐
│ STT v3 │ │  LLM  │ │  TTS v3 │
│ Yandex │ │ +RAG  │ │  Yandex │
└────────┘ └───────┘ └─────────┘
                │
         ┌──────▼─────┐
         │ PostgreSQL │
         └────────────┘
```

#### Задачи

**Backend:**
1. **Yandex SpeechKit v3 Streaming адаптеры**
   - [ ] `YandexSTTTranscriber` - адаптер для Vocode
   - [ ] `YandexTTSSynthesizer` - адаптер для Vocode
   - [ ] Получение IAM token для Yandex v3 API

2. **Vocode Agent с RAG**
   - [ ] `HostessAgent` - кастомный агент с интеграцией YandexGPT + RAG
   - [ ] Конфигурация промптов для агента
   - [ ] Интеграция с conversation_manager (сохранение в БД)

3. **WebRTC endpoint (браузер)**
   - [ ] FastAPI endpoint для WebRTC signaling
   - [ ] Vocode WebRTC телефония
   - [ ] UI компонент для голосовых звонков в админке

4. **SIP endpoint (Beeline телефония)**
   - [ ] Настройка SIP сервера (встроенный в Vocode)
   - [ ] Регистрация на Beeline SIP
   - [ ] Обработка входящих звонков
   - [ ] Маршрутизация звонков на Vocode Agent

5. **Call Management**
   - [ ] Создание conversation при начале звонка
   - [ ] Логирование метаданных (канал: webrtc/sip, номер телефона)
   - [ ] Dashboard для аналитики звонков

**Инфраструктура:**
1. **Docker контейнер**
   ```yaml
   vocode:
     image: vocodedev/vocode:latest
     ports:
       - "5060:5060/udp"  # SIP
     environment:
       - SIP_SERVER=${BEELINE_SIP_SERVER}
       - SIP_USERNAME=${BEELINE_SIP_USER}
       - SIP_PASSWORD=${BEELINE_SIP_PASS}
   ```

2. **Firewall правила**
   - Открыть порт 5060 UDP для SIP
   - Настроить RTP порты (10000-20000)

**Конфигурация:**
```bash
# .env
BEELINE_SIP_SERVER=sip.beeline.ru
BEELINE_SIP_USER=ваш_логин
BEELINE_SIP_PASS=ваш_пароль
BEELINE_PHONE_NUMBER=+7XXXXXXXXXX
```

**Зависимости:**
```python
vocode-core==0.1.108
pydantic==2.5.3  # уже есть
websockets==12.0  # уже есть
```

**Frontend:**
1. **WebRTC Voice Interface**
   - [ ] Компонент для WebRTC звонков (заменит текущий VoiceRecorder)
   - [ ] Индикатор статуса (слушает / думает / говорит)
   - [ ] Кнопка прерывания бота
   - [ ] Визуализация звуковых волн (опционально)

**Тестирование:**
1. **Локальное (WebRTC в браузере)**
   - Тестирование диалогов в админке
   - Проверка VAD и прерываний
   - Латентность и качество звука

2. **Продакшн (Beeline SIP)**
   - Тестовые звонки с реального телефона
   - Нагрузочное тестирование (несколько одновременных звонков)
   - Интеграция с CRM (опционально)

**Срок:** 3-4 дня

---

## 📊 Метрики и аналитика (будущее)

- [ ] Dashboard для мониторинга звонков
- [ ] Статистика бронирований
- [ ] Анализ успешности диалогов
- [ ] A/B тестирование промптов
- [ ] Интеграция с CRM системой ресторана

---

## 💰 Оценка стоимости (месячная)

### Инфраструктура
- VPS (4 CPU, 8GB RAM): ~1500₽/мес
- Домен + SSL: ~500₽/год

### API сервисы (при ~100 звонках/месяц, средняя длительность 3 мин)
- Beeline SIP: 500₽ за номер + ~600₽ за звонки (2₽/мин × 300 мин)
- Yandex SpeechKit Streaming: ~2000₽ (40₽/час × 5 часов)
- YandexGPT: ~500₽
- OpenAI Embeddings: ~100₽

**Итого:** ~5200₽/месяц

---

## 🎯 Следующие шаги

### Сейчас (14 ноября 2024):
1. **Начинаем Фазу 4: RAG**
   - Создание моделей Document и DocumentChunk
   - ChromaDB интеграция
   - API для загрузки документов
   - Админка для управления базой знаний

### После RAG:
2. **Фаза 5: Vocode** (WebRTC для отладки + SIP для продакшн звонков)

---

## 📝 Заметки

- Используем YandexGPT для основного LLM (уже настроен)
- Streaming API v3 требует IAM token вместо API key
- ChromaDB будет локально в Docker (позже можно мигрировать на Qdrant Cloud)
- Frontend build требует пересборки при изменении WebSocket URL
- Nginx конфигурация на сервере должна поддерживать WebSocket Upgrade
